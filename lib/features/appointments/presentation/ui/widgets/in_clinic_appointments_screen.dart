import 'package:flutter/material.dart';

class InClinicAppointmentsScreen extends StatelessWidget {
  const InClinicAppointmentsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("مواعيد في العيادة")); // In Clinic
  }
}