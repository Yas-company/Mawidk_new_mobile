import 'package:flutter/material.dart';

class RemoteAppointmentsScreen extends StatelessWidget {
  const RemoteAppointmentsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("مواعيد عن بعد")); // Remote
  }
}
